import { VoiceInputOverlay } from "@/components/chat/VoiceInputOverlay";

const VoiceChatPage = () => {
  return (
    <main className="flex min-h-dvh w-full flex-col justify-center">
      <div className="flex w-full flex-1 items-center justify-center px-4 py-2.5"></div>
    </main>
  );
};

export default VoiceChatPage;
